#include<stdio.h>
#include<math.h>
int main()
{
int a,i,count;
for(i=2;i<=(sqrt(a));i++)
{
if(a%i==0)
count++;
}
if(count==0)
printf("the no. is prime");
else
printf("the no is not a prime ");

return 0;
}  
